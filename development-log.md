# Development Log

> 本文件用于记录开发过程，重点服务于项目后期答辩、过程复盘和成果展示。它不同于 `progress.md`：`progress.md` 关注任务状态，本文件关注“我们做了什么、为什么这样做、遇到了什么问题、如何解决、答辩时如何讲清楚”。

## 使用方式

每完成一个阶段或重要功能后，建议补充以下内容：

- 本阶段目标。
- 实际完成内容。
- 采用的技术方案。
- 为什么选择该方案。
- 遇到的问题。
- 解决办法。
- 可用于答辩展示的亮点。
- 尚未完成或后续可优化的地方。

建议每次开发结束后追加一条“阶段记录”或“功能记录”，不要只在项目结束时补写。这样答辩时能更自然地说明开发过程。

## 项目基本信息

- 项目名称：本地文档管理系统。
- 参考项目：paperless-ngx。
- 项目类型：Java Web 课程实训项目。
- 代码仓库：`https://github.com/Heria-Cris/Paperless-ngx-Project.git`。
- 开发策略：先落地基础可访问界面，再逐步完善真实业务功能。
- 主要技术：Spring Boot 3、Spring MVC、Thymeleaf、Session、HandlerInterceptor、MyBatis-Plus、MySQL、本地文件系统。
- 核心目标：实现文档上传、分类、标签、查询、下载、权限控制、统计和基础日志。

凭据说明：

- GitHub Personal Access Token 属于敏感凭据，不记录在本文件或任何项目文件中。
- 后续提交代码时可以本地提交；推送到 GitHub 建议使用 Git Credential Manager、GitHub Desktop、IDE 登录或用户手动输入 token。

## 开发策略说明

本项目采用“先落地，再完善”的开发策略。

原因：

- 课程实训项目需要尽早形成可运行、可演示的系统。
- 先做界面可以帮助团队统一页面结构、菜单、路由和交互流程。
- 页面骨架完成后，各成员可以围绕明确入口并行开发后端功能。
- 前期使用模拟数据能减少数据库和业务逻辑未完成带来的阻塞。
- 后期再逐步替换为真实数据库、真实文件上传下载和权限控制，风险更可控。

答辩表达：

> 我们没有一开始就直接堆复杂功能，而是先完成可访问的系统原型，确定整体页面和业务流程，再逐步把模拟数据替换为真实数据，把页面操作接入数据库、文件系统和权限控制。这样能保证项目每个阶段都有可运行成果，也方便团队协作和阶段性验收。

## 阶段记录

### 阶段 0：需求分析与开发上下文初始化

日期：2026-06-26

阶段目标：

- 阅读项目文档。
- 明确项目范围、技术栈、核心模块和不做内容。
- 建立后续开发可复用的上下文文档。

完成内容：

- 根据 `第一组--项目文档.docx` 提炼项目背景。
- 创建 `project-context.md`，用于保存长期项目背景。
- 创建 `progress.md`，用于同步开发进度。
- 创建 `development-plan.md`，用于确定开发顺序。
- 创建 `development-log.md`，用于记录开发过程和答辩素材。

技术与方案：

- 使用 Markdown 管理项目上下文和开发记录。
- 将项目文档拆分为背景、进度、开发方案、开发日志四类文档，分别承担不同用途。

方案理由：

- `project-context.md` 保持项目背景稳定，避免每次对话重新解释需求。
- `progress.md` 跟踪任务状态，方便跨对话继续开发。
- `development-plan.md` 统一开发顺序，避免多人开发时方向混乱。
- `development-log.md` 沉淀答辩过程材料，避免后期临时补写。

遇到的问题：

- 原始文档内容较长，直接作为开发上下文不利于快速定位。

解决办法：

- 将原始 PRD 提炼为多个用途明确的 Markdown 文件。

答辩可讲点：

- 项目前期进行了需求裁剪，明确不实现 OCR、Redis、Elasticsearch、异步任务等超出课程范围的复杂能力。
- 采用文档化方式管理项目背景、进度和开发过程，提高团队协作效率。

后续优化：

- 每完成一个开发阶段后，继续补充本文件。
- 在功能完成后补充界面截图、关键代码说明和演示流程。

### 阶段 0：项目骨架与运行入口

日期：2026-06-26

阶段目标：

- 创建可以启动的 Spring Boot Web 项目。
- 提供一个基础首页访问入口。
- 确认 Maven 构建和页面访问正常。

完成内容：

- 创建 `pom.xml`，引入 Spring Boot Web、Thymeleaf、Validation、Lombok、Hutool 和测试依赖。
- 创建 `.gitignore`，排除 `target/`、日志、IDE 文件和上传目录。
- 创建项目启动类 `PaperlessLocalApplication`。
- 创建 `HomeController`，支持 `/` 和 `/dashboard` 访问首页。
- 创建 `application.yml`，配置端口、Thymeleaf、上传大小和应用上传目录。
- 创建 `dashboard.html`，作为阶段 0 的基础首页。
- 创建 `app.css`，提供后台管理系统的基础布局和样式。

技术与方案：

- 使用 Spring Boot 3 作为后端基础框架。
- 使用 Thymeleaf 渲染首页，符合项目服务端渲染的技术路线。
- 阶段 0 暂不引入 MyBatis-Plus 和 MySQL 依赖，避免未配置数据源导致应用无法启动。
- 页面先展示基础统计占位数据，为阶段 1 的界面落地和后续真实统计预留结构。

方案理由：

- 阶段 0 的核心是“能启动、能访问”，不应被数据库配置阻塞。
- 首页先采用简单后台布局，后续登录页、文档列表、分类标签页面可以沿用同一视觉方向。
- 将数据库相关依赖推迟到阶段 3，更符合“先落地、再完善”的开发方案。

验证结果：

- 执行 `mvn package` 构建成功。
- 临时启动 `target/paperless-local-0.0.1-SNAPSHOT.jar` 后访问 `http://localhost:8080/dashboard`，返回 HTTP 200。
- 页面内容包含“阶段 0 已完成”，说明 Thymeleaf 模板渲染正常。

遇到的问题：

- 如果过早加入 MyBatis-Plus 和 MySQL 依赖，Spring Boot 可能会在没有数据源配置时启动失败。

解决办法：

- 阶段 0 仅保留 Web、模板、校验和工具类依赖。
- 将 MyBatis-Plus、MySQL 数据源配置放到阶段 3 接入数据库时处理。

答辩可讲点：

- 项目从第一步就保证可运行、可访问，而不是只停留在代码结构。
- 技术依赖按阶段引入，避免未完成模块影响当前系统启动。
- 首页虽然是基础版本，但已经体现后台管理系统的布局方向，为后续模块页面统一奠定基础。

后续可优化：

- 阶段 1 增加登录页、文档列表页、上传页、分类页、标签页等基础界面。
- 抽取公共布局模板，减少页面重复代码。
- 使用模拟数据完善首页统计和最近文档列表。

### 阶段 1：基础可访问界面

日期：2026-06-26

阶段目标：

- 参考 Paperless-ngx 官方截图，先完成基础可访问、可点击、风格统一的后台界面。
- 页面先使用模拟数据，不接数据库和真实业务逻辑。
- 为后续 Session 登录、文档 CRUD、上传下载、分类标签管理预留页面入口。

参考资料：

- Paperless-ngx 官方截图：https://docs.paperless-ngx.com/#screenshots
- 用户提供的 Dashboard、Documents 网格视图、Documents 列表视图截图。

完成内容：

- 创建登录页 `/login`。
- 将首页 `/dashboard` 改造为 Paperless-ngx 风格仪表盘。
- 创建文档列表页 `/documents`，包含筛选工具条、视图按钮和模拟数据表格。
- 创建上传页 `/documents/upload`，包含拖拽上传区域和文档元数据表单。
- 创建文档详情页 `/documents/{id}` 和编辑页 `/documents/{id}/edit`。
- 创建分类管理页 `/categories`。
- 创建标签管理页 `/tags`。
- 创建用户管理页 `/users`。
- 创建日志页 `/logs`。
- 重写 `app.css`，实现深绿色顶栏、浅色侧边栏、标签色块、表格、面板和表单样式。

技术与方案：

- 阶段 1 使用单个 `app.html` 模板，通过 `activePage` 控制不同页面区域展示。
- Controller 中使用 Java record 和 `List.of(...)` 提供模拟数据。
- 登录页单独使用 `login.html`，便于阶段 2 接入真实登录逻辑。
- 页面路由先完整落地，按钮和表单先作为静态交互入口。

方案理由：

- 单模板方案能快速统一页面风格，减少早期重复模板维护成本。
- 模拟数据让页面在未接数据库前也能完整展示系统形态。
- 先落地路由和页面入口，便于团队成员后续按模块替换为真实功能。

遇到的问题：

- 需要在课程项目复杂度内尽量贴近 Paperless-ngx 的界面，但不能照搬其完整前端架构。

解决办法：

- 保留 Paperless-ngx 的视觉特征：深绿色顶栏、左侧导航、筛选工具条、密集表格、彩色标签。
- 不引入 Angular 或完整前后端分离，只用 Thymeleaf 和 CSS 实现阶段 1 界面。

答辩可讲点：

- 项目界面参考成熟开源系统 Paperless-ngx，但技术实现进行了课程化简化。
- 阶段 1 已经形成完整系统雏形，能演示主要模块入口。
- 页面和路由先行，使后续功能开发有明确落点，也方便团队分工。

后续可优化：

- 抽取 Thymeleaf 公共布局片段。
- 为文档列表增加卡片视图切换。
- 接入真实登录后根据用户角色控制菜单显示。
- 接入数据库后将模拟数据替换为真实查询结果。

验证结果：

- 执行 `mvn package` 构建成功。
- 临时启动 Jar 后，以下路由均返回 HTTP 200：`/login`、`/dashboard`、`/documents`、`/documents/upload`、`/documents/0`、`/documents/0/edit`、`/categories`、`/tags`、`/users`、`/logs`。

### 阶段 2：基础登录与页面访问控制

日期：2026-06-26

阶段目标：

- 接入基础登录认证。
- 使用 Session 保存登录状态。
- 实现退出登录。
- 使用 HandlerInterceptor 拦截未登录访问。
- 区分管理员和普通用户的基础页面权限。

完成内容：

- 新增 `LoginUser`，用于保存当前登录用户的用户名、展示名和角色。
- 新增 `AuthService`，使用内存账号完成阶段 2 登录校验。
- 新增 `SessionKeys`，统一管理 Session key。
- 新增 `AuthController`，处理登录页、登录提交和退出登录。
- 新增 `AuthInterceptor`，处理未登录拦截和管理员页面权限判断。
- 新增 `WebMvcConfig`，注册登录拦截器并放行登录页和静态资源。
- 修改登录页，改为 POST `/login`，支持错误提示和退出提示。
- 修改后台模板，显示当前用户、角色和退出入口。
- 普通用户隐藏管理员菜单，访问管理页面时跳转回首页并显示无权限提示。

测试账号：

- 管理员：`admin` / `admin123`
- 普通用户：`user` / `user123`

技术与方案：

- 阶段 2 继续不接数据库，先使用内存账号完成认证闭环。
- 使用 Session 保存 `LoginUser` 对象。
- 使用 HandlerInterceptor 统一处理登录状态和简单权限判断。
- 管理员页面暂定为 `/categories`、`/tags`、`/users`、`/logs`。

方案理由：

- 当前阶段重点是验证登录流程和页面访问控制，不让数据库设计阻塞认证闭环。
- Session 与 Thymeleaf 服务端渲染配合简单，符合项目技术方案。
- HandlerInterceptor 比 Spring Security 更轻量，适合课程实训基础权限需求。

验证结果：

- 执行 `mvn package` 构建成功。
- 未登录访问 `/dashboard` 返回 302 并跳转 `/login`。
- 使用 `admin/admin123` 登录返回 302 并跳转 `/dashboard`。
- 管理员访问 `/users` 返回 HTTP 200。
- 使用 `user/user123` 登录返回 302 并跳转 `/dashboard`。
- 普通用户访问 `/tags` 返回 302 并跳转 `/dashboard?denied`。
- 访问 `/logout` 后返回 302 并跳转 `/login?logout`。

遇到的问题：

- 需要在未接数据库的情况下实现真实可用的登录状态。

解决办法：

- 使用内存账号作为阶段性方案，后续阶段 3 接入数据库后再替换为用户表查询。

答辩可讲点：

- 系统已经不是单纯静态页面，而是具备基础登录状态和权限边界。
- 通过 Session 保存当前用户，通过拦截器统一控制页面访问。
- 管理员与普通用户的权限差异已经能在界面和路由层面体现。

后续可优化：

- 阶段 3 设计用户表后，将内存账号替换为数据库用户。
- 增加密码哈希，避免明文密码。
- 为更多功能按钮增加角色级别的显示控制。

## 功能开发记录模板

后续每实现一个功能，可以复制以下模板追加记录。

```markdown
### 功能名称：XXX

日期：YYYY-MM-DD

负责人：

相关文件：

- `src/main/java/...`
- `src/main/resources/templates/...`

功能目标：

- 

完成内容：

- 

技术实现：

- 

关键流程：

1. 
2. 
3. 

遇到的问题：

- 

解决办法：

- 

答辩可讲点：

- 

后续可优化：

- 
```

## 问题与解决记录

| 日期 | 问题 | 原因 | 解决办法 | 答辩说明 |
| --- | --- | --- | --- | --- |
| 2026-06-26 | 原始需求文档较长，不便于后续对话直接使用 | 内容包含背景、功能、分工、非功能需求等多个层次 | 拆分为项目背景、开发进度、开发方案、开发过程记录四类 Markdown | 前期通过文档化降低沟通成本，便于团队协作 |

## 技术选型说明

### 为什么使用 Spring Boot 单体架构

- 课程实训项目更关注 CRUD、页面流转、权限控制和文件管理闭环。
- 单体架构部署和调试成本低。
- 团队成员可以按照模块分工开发 Controller、Service、Mapper 和页面。
- 相比完整前后端分离，Thymeleaf 服务端渲染减少联调复杂度。

答辩表达：

> 项目定位是课程实训，不追求复杂微服务或完整前后端分离。我们选择 Spring Boot 单体架构，是为了把重点放在文档管理核心业务、权限控制和文件处理上，同时保证项目可运行、可维护、可展示。

### 为什么使用 Session 而不是 Token 或 Spring Security

- 项目只需要基础登录认证和角色区分。
- Session 与服务端渲染页面配合简单。
- HandlerInterceptor 可以清晰实现未登录拦截和基础权限判断。
- 避免引入 Spring Security 复杂配置，降低学习和实现成本。

答辩表达：

> 我们的权限需求主要是区分管理员和普通用户，不涉及复杂 RBAC，因此采用 Session 加拦截器实现登录控制，既能满足需求，也符合课程项目复杂度。

### 为什么先使用本地文件系统

- 项目目标是本地文档管理系统。
- 本地存储实现简单，便于演示上传和下载。
- 数据库存储元数据，文件系统保存真实文件，职责清晰。
- 避免引入对象存储、分布式文件系统等额外复杂度。

答辩表达：

> 系统将文件实体保存在服务器本地磁盘，数据库只保存路径和元数据。这样既能保证文件上传下载功能真实可用，又能保持数据库结构清晰。

### 为什么不做 OCR、Elasticsearch 和异步任务

- OCR 依赖外部工具和复杂文档处理流程。
- Elasticsearch 超出基础 CRUD 项目范围。
- 异步任务通常需要 Redis 或任务队列，增加部署和调试成本。
- 本项目更关注文档上传、分类、标签、搜索、权限和下载的基础闭环。

答辩表达：

> paperless-ngx 是成熟复杂的文档管理系统，但课程实训需要合理裁剪。我们保留文档管理核心思想，去掉 OCR、Elasticsearch、异步任务等复杂能力，保证项目范围可控、功能完整可演示。

## 答辩演示主线

建议答辩按以下顺序演示：

1. 说明项目背景：本地文件夹管理存在分类混乱、检索困难、权限缺失等问题。
2. 说明项目目标：实现简化版本地文档管理系统。
3. 展示登录页：说明 Session 登录和未登录拦截。
4. 展示首页统计：说明管理员和普通用户看到的数据范围不同。
5. 展示文档上传：说明文件保存到本地磁盘，数据库保存元数据。
6. 展示文档列表：说明分页、分类、标签、关键词筛选。
7. 展示文档详情和编辑：说明文档元数据管理。
8. 展示下载或预览：说明权限校验和文件访问。
9. 展示分类和标签管理：说明文档组织方式。
10. 展示操作日志：说明关键操作可追踪。
11. 总结技术选型和项目裁剪：说明为什么使用 Spring Boot 单体、Thymeleaf、Session、本地文件系统。

## 答辩亮点池

后续开发中可持续补充。

- 需求裁剪清晰：参考 paperless-ngx，但根据课程范围去掉复杂功能。
- 开发策略稳健：先完成可访问界面，再逐步接入真实功能。
- 权限边界明确：管理员管理全局，普通用户只能管理个人文档。
- 文件与元数据分离：文件存本地，数据库存路径和信息。
- 分类与标签结合：分类是一对多，标签是多对多。
- 使用逻辑删除和回收站思路，避免误删。
- 通过操作日志增强可追踪性。
- 采用 Markdown 文档维护背景、计划、进度和过程，便于团队协作。

## 待补充材料

- 项目启动截图。
- 登录页截图。
- 首页截图。
- 文档列表截图。
- 上传流程截图。
- 数据库 ER 图。
- 系统架构图。
- 核心代码截图或说明。
- 测试账号和演示数据。
- 最终答辩讲稿提纲。
